## What

<!-- One or two sentences describing what this PR changes. -->

## Why

<!-- Why is this change needed? Link any related issues with "Fixes #N". -->

## How

<!-- Briefly describe the approach if it isn't obvious from the diff. -->

## Checklist

- [ ] `task test` passes
- [ ] `task lint` passes
- [ ] `task web-test` and `task web-check` pass (if frontend changed)
- [ ] `task build` succeeds (full embedded build)
- [ ] README / Taskfile updated if a new developer workflow was added
- [ ] No new dependencies without explanation
